# Configuration for git-buildpackage and affiliated tools

[DEFAULT]
debian-branch = debian/trixie
pristine-tar = True
sign-tags = True
upstream-branch = upstream/trixie
upstream-tag = upstream/%(version)s_trixie

[import-orig]
merge-mode = replace
